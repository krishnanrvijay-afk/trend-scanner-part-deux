# Hyperliquid private key for live trading (not needed in PAPER_MODE)
HL_PRIVATE_KEY=0x...

# Hyperliquid wallet address (used to fetch positions in live mode)
HL_WALLET_ADDRESS=0x...

# Set to false to enable live trading (default is true = paper mode)
PAPER_MODE=true
